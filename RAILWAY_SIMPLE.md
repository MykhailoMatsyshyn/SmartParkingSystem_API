# 🚀 Простий деплой на Railway

## ✅ Що потрібно

Railway автоматично визначить Python проект, якщо є:
- ✅ `requirements_api.txt` - залежності
- ✅ `Procfile` - команда запуску
- ✅ `sensor_api_server.py` - основний файл

**Всі ці файли вже є!** ✅

---

## 📋 Налаштування в Railway

### 1. Root Directory
**Settings → Source → Root Directory:** `sensor-api-server`

### 2. Start Command
**Settings → Deploy → Start Command:** `python sensor_api_server.py`

### 3. Variables (опціонально)
**Variables → Add Variable:**
- `PORT` = `5000` (Railway автоматично надасть, але можна вказати)

---

## 🔄 Перезапустіть деплой

1. **Deployments → Redeploy**
2. Або зробіть новий commit і push

---

## ✅ Очікуваний результат

Nixpacks автоматично:
- ✅ Визначить Python проект
- ✅ Встановить Python 3.10
- ✅ Встановить залежності з `requirements_api.txt`
- ✅ Запустить сервер

---

## 🐛 Якщо все ще помилка

1. **Перевірте Root Directory** - має бути `sensor-api-server`
2. **Перевірте логи** - Railway dashboard → Deployments → View Logs
3. **Спробуйте видалити і створити проект заново**

---

## 💡 Порада

Не створюйте `nixpacks.toml` або `railway.json` - дозвольте Railway автоматично визначити проект!

