# REST API сервер для генерації даних сенсорів

**Це окремий компонент системи, який має запускатися окремо від Android додатку.**

## 📋 Опис

REST API сервер, який генерує емульовані дані сенсорів для системи моніторингу паркування. Сервер імітує роботу реальних датчиків і надає дані через HTTP API.

## 🚀 Швидкий старт

### 1. Встановлення

```bash
# Перейдіть в директорію сервера
cd sensor-api-server

# Встановіть залежності
pip install -r requirements_api.txt
```

### 2. Запуск

```bash
python sensor_api_server.py
```

Сервер запуститься на `http://localhost:5000`

## 📡 API Endpoints

### Компонент 1: Дані сенсорів

#### GET /api/sensor-data

Отримати поточні дані сенсорів

**Відповідь:**

```json
{
  "timestamp": 1234567890000,
  "parking_sensors": [0,1,0,1,1,...],
  "parking_occupied": 0.5,
  "free_spots": 50,
  "co_level": 120.5,
  "nox_level": 80.3,
  "temperature": 18.2
}
```

#### GET /api/sensor-data/stream

Потокова передача даних (Server-Sent Events)
Оновлює дані кожні 5 секунд

### Компонент 3: Керування пристроями

#### GET /api/devices

Отримати стан всіх пристроїв

#### GET /api/devices/{deviceId}

Отримати стан конкретного пристрою

#### PUT /api/devices/{deviceId}

Оновити стан пристрою (синхронізується з Firebase)

**Приклад:**

```bash
curl -X PUT http://localhost:5000/api/devices/direction_panels_1 \
  -H "Content-Type: application/json" \
  -d '{"enabled": true, "brightness": 100}'
```

### GET /api/health

Перевірка стану сервера

## 🔧 Налаштування для Android додатку

### Для Android емулятора:

- IP адреса: `10.0.2.2` (спеціальна адреса для доступу до localhost хоста)
- URL: `http://10.0.2.2:5000`

### Для реального Android пристрою:

1. Знайдіть IP адресу вашого комп'ютера:
   - **Windows**: `ipconfig` (шукайте IPv4 Address)
   - **Linux/Mac**: `ifconfig` або `ip addr`
2. Змініть `BASE_URL` в `ApiClient.kt` Android проекту:
   ```kotlin
   private const val BASE_URL = "http://YOUR_IP:5000/"
   ```
3. Переконайтеся, що пристрій і комп'ютер в одній Wi-Fi мережі

## 📝 Структура проекту

```
sensor-api-server/
├── sensor_api_server.py    # Основний файл сервера
├── requirements_api.txt    # Python залежності
└── README.md              # Ця інструкція
```

## ⚙️ Налаштування

### Firebase синхронізація (опціонально)

Для синхронізації з Firebase:

1. Отримайте Service Account Key з Firebase Console
2. Збережіть як `firebase-credentials.json` в `sensor-api-server/`
3. Див. `FIREBASE_SETUP_PYTHON.md` для деталей

**Переваги:**

- ✅ Стан пристроїв зберігається в Firebase
- ✅ Синхронізація між Python сервером та Android пристроями
- ✅ Стан не втрачається при перезапуску сервера

**Без Firebase:**

- ⚠️ Стан зберігається тільки в пам'яті
- ⚠️ Втрачається при перезапуску сервера

### Параметри генерації

Можна змінити в `sensor_api_server.py`:

- Інтервал оновлення даних
- Діапазони значень сенсорів
- Частота аномалій

## 🐛 Troubleshooting

### Помилка "Connection refused"

- Перевірте, що сервер запущено
- Перевірте IP адресу
- Перевірте firewall налаштування

### Помилка "Port already in use"

- Змініть порт в `sensor_api_server.py`: `app.run(host='0.0.0.0', port=5001)`
- Оновіть `BASE_URL` в Android додатку

### Дані не оновлюються

- Перевірте логи сервера
- Перевірте логи мобільного додатку (HTTP запити)

## 📚 Альтернативи

### MQTT (Message Broker)

Можна використати MQTT брокер (наприклад, Mosquitto) замість REST API:

- Тема: `parking/sensors/data`
- Формат: JSON
- Мобільний додаток підписується на тему

### WebSocket

Реалізувати WebSocket сервер для real-time передачі даних

## ⚠️ Важливо

- Це **окремий компонент**, не частина Android проекту
- Сервер має бути запущений **окремо** на комп'ютері
- Для роботи Android додатку потрібен доступ до сервера через мережу
